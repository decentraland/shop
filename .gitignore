# Dependencies
node_modules/

# Builds
dist/
build/
*.tsbuildinfo
# Compiled from vite.config.ts by `tsc -b` — vite loads the .ts directly, the .js is a stray artifact.
app/vite.config.js

# Tests / coverage
coverage/

# Env — commit only *.env.example (real/local configs are ignored, incl. dev secrets)
.env
.env.local
.env.*.local
.env.default

# Tool run output
tools/*/out/

# Editor / OS
.DS_Store
*.log
npm-debug.log*
.idea/
.vscode/

# Internal design / vision / analysis docs — never public.
design/
/VISION.md
/ROADMAP.md
