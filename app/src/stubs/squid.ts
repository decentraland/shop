// Stub for @0xsquid/sdk — an optional cross-chain peer dep of decentraland-transactions that the
// Shop never uses. Aliased in vite.config.ts so the bundler doesn't try to resolve the real package.
export class Squid {}
export const SquidCallType = {}
export const ChainType = {}
export default {}
