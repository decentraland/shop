// Creator store settings (/store-settings). Scaffolding only — reached from the pen button
// on the creator hero (CreatorHero). The actual edit flow (cover image, description, links, …) lands
// in a follow-up task; for now this route renders an empty shell so the button has a destination.
export function StoreSettings() {
  return (
    <section className="store-settings" aria-label="Store settings">
      Store settings
    </section>
  )
}

export default StoreSettings
